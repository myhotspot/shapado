-cache("tag_cloud_#{current_group.id}_#{Date.today.to_s}") do
  .widget-tags.module
    %section
      %hgroup
        %ul
          %li.active
            =link_to "Популярность"
          %li
            =link_to "Алфавит"
      .tag_cloud
        = tag_cloud([], {}, widget[:settings][:limit])
    .clear
